package guiao01;

public class ExpressionTree {
    Node root = new Node(null);

    public Node add(String value) {
        // TODO
        // encontrar operador sem todos os filhos e meter la numero
        // com operador é fazer random Node por baixo de numero acho 
        return new Node(value);
    }
}
