#!/usr/bin/bash
# the following command sends to the terminal display window
# the constents of file "/etc/passwd"
cat /etc/passwd
# the following command piping sends to the terminal display window
# the number of lines of file "/etc/passwd"
cat /etc/passwd | wc -l
